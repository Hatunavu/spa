import 'package:flutter/material.dart';

class MemberRankWidget extends StatelessWidget {
  const MemberRankWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
